import { DurableObject } from 'cloudflare:workers'
import type { Env } from '../env'

// Placeholder — real implementation lands in Task 12
export class SubscriptionActor extends DurableObject<Env> {}
