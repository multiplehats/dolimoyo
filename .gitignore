node_modules
dist
.turbo
.wrangler
*.tsbuildinfo
.DS_Store

# Wrangler local secrets (generated from .env via predev)
.dev.vars

# dotenvx private keys (NEVER commit)
.env.keys

# Local overrides (gitignored)
.env*.local

# Conductor workspace overrides
.env.conductor

# Encrypted .env / .env.production / .env.example files ARE safe to commit.
